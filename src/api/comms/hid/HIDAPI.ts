const find = () => {
  const arr: Array<number> = [];
  return arr;
};

const connected = () => {};

const isDeviceConnected = () => {};

const isDeviceSupported = () => {};

export { find, connected, isDeviceConnected, isDeviceSupported };
